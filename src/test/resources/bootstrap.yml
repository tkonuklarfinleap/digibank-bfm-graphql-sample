spring:
  application:
    name: ${APP_NAME:nova-os-infinitec}
  cloud:
    config:
      enabled: false
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration
      - org.springframework.cloud.stream.config.BindingServiceConfiguration
